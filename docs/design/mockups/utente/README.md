# Mockup e riferimenti dell'utente

Screenshot e fotomontaggi passati a mano durante il redesign (sezione Amici,
notifiche a banner, ecc.). Stanno qui e **non** in `public/`: tutto ciò che
sta in `public/` finisce nel precache del service worker, quindi ogni
installazione della PWA se lo scaricherebbe (erano 5,3 MB per quattro
immagini mai referenziate dal codice).
