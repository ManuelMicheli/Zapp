/**
 * Larghezza dell'etichetta davanti a una fila di filtri della home ("Per genere",
 * "Per piattaforma"). Sta qui e non in due file perché serve a tutte e due: ogni
 * gruppo etichetta + pillole è centrato per conto suo, quindi due etichette di
 * larghezza diversa fanno partire le due file da due x diverse.
 */
export const NAV_FILTRO_LABEL = 128;
