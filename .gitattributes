# Fine riga: LF ovunque, indice e copia di lavoro. Prettier scrive LF, quindi con
# la conversione CRLF di Windows git vedeva ~300 file "modificati" con diff zero a
# ogni `git status`. Serve anche `git config core.autocrlf false` nel clone locale.
* text=auto eol=lf

# Binari: mai conversione, mai diff testuale.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
*.pdf binary

# Generati: fuori dalle statistiche del linguaggio e dai diff rumorosi.
pnpm-lock.yaml -diff linguist-generated
src/types/database.ts linguist-generated
